/**
 * @file
 * Contains js for the accordion example.
 */

window.app = window.app || {};

/*
 * SelfQuiz object
 */
window.app.selfquiz = {
	initialize : function(config) {
		console.log('selfquiz initialized');
	}
}